{% liquid
  assign heading_text = heading | default: heading
  assign heading_tag = tag | default: 'h2'
  assign heading_variant = variant | default: 'u-text-style-h2'
%}
                        {% if heading_text != blank %}
                        <{{ heading_tag="" }}="" class="{{ heading_variant }}">
                          {{ heading_text }}
                        <!--{{-->
                        {% endif %}
