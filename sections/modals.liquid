<div class="u-display-none">
          
          
          
          
        </div>
        
        <div class="u-container"></div>
        {% liquid assign eyebrow_text = eyebrow assign eyebrow_variant = variant | default: 'u-eyebrow-main' assign eyebrow_custom_class = custom_class assign eyebrow_classes = 'u-eyebrow-text' if eyebrow_variant != blank assign eyebrow_classes = eyebrow_classes | append: ' ' | append: eyebrow_variant endif if eyebrow_custom_class != blank assign eyebrow_classes = eyebrow_classes | append: ' ' | append: eyebrow_custom_class endif %} {% schema %} {
    "tag": "div",
    "name": "Modals",
    "class": "modals_wrap",
    "settings": [
        {
            "type": "header",
            "content": "Sektion"
        },
        {
            "type": "text",
            "id": "section_id",
            "label": "ID"
        }
    ],
    "presets": [
        {
            "name": "Modals",
            "category": "Liquify",
            "blocks": []
        }
    ]
} {% endschema %}